# 複製這個檔案為 .env 並依實際情況修改，.env 本身不進版控（含機器相關本機設定）

# HTTP 服務埠（員工端網頁呼叫 http://localhost:<HTTP_PORT>）
HTTP_PORT=3399

# 序列埠：裝置管理員 → 連接埠(COM 與 LPT) → 插上 USB 轉 RS-232 轉接線後才會出現，例如 COM3
SERIAL_PORT=COM3

# 鮑率（已實測 WP-560 用 9600，不需更動）
BAUD=9600

# 這台櫃檯電腦預設所屬館別（hsinchu / shilin），print 呼叫未帶 gymId 時的備援值
DEFAULT_GYM=hsinchu

# 允許呼叫本代理的網頁來源（逗號分隔）；正式環境務必包含 staff 網域
ALLOWED_ORIGINS=https://staff.redrocktaiwan.com,http://localhost:5173

# 通常不用改：極少數受限環境 PATH 裡沒有 powershell.exe 才需要填完整路徑，例如
# C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe
# POWERSHELL_EXE=powershell.exe

# 常駐序列埠連線（2026-08-15 新增）：開機時只開一次序列埠、之後所有請求共用同一個常駐行程，
# 避免每次查詢/列印都要重新開埠（這台機器約 6 秒）。預設開啟。
# 如果裝上去之後發票列印/連線狀態查詢一直異常（跟這次改版之前不同的新問題），可以先把這個設成
# false 退回舊的「每次都重新開埠」行為（較慢，但已知可行、是先前一路驗證過的版本）暫時維持營運，
# 再回報問題內容（連同 bridge-debug.log）。
# PERSISTENT_MODE=false

# 常駐模式下單一命令的逾時（毫秒）。通常不用改——常駐模式下序列埠已經開好，這是正常操作
# 應該要的時間上限，不是「開埠」那個慢步驟。
# DAEMON_CMD_TIMEOUT_MS=5000
# DAEMON_PRINT_TIMEOUT_MS=10000
