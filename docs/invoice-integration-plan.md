# 發票機串接架構設計（WinPOS WP-560 二聯式發票機）

> 目標：把現有「每日結帳手動抄發票號碼」升級為**系統直接驅動實體發票機列印統一發票**，並在收款當下自動記下發票號、結帳起訖號自動帶出。
> 決策（2026-07-21 拍板）：**實體二聯式紙本發票**（不走電子發票）＋**本地列印代理**橋接＋號碼**後端計數**＋收款當下開票＋二聯**選填統編**。
> 狀態：**架構已定，待硬體到位進 PoC**。使用者採購舊發票機＋RS-232轉USB＋二聯練習紙捲；下一步 P1＝本地代理 + 列印對位 PoC（見第 6 節）。

---

## 0. 現況（2026-07 快照）

- 系統**本身不開發票**。實體發票機印統一發票，店員在**每日結帳**（`DailySettlementPage`）手動輸入發票起訖號、作廢號、作廢金額。
- 系統僅收下號碼供報表：`dailySettlements` 存 `invoiceSegments`(多段起訖) / `invoiceStartNumber` / `invoiceLastNumber` / `invoiceVoidNumbers` / `voidInvoiceAmount`；產「月銷售紀錄」與「發票明細表」（仿二聯式收銀機統一發票明細表）。
- 收款點（未來要掛開票）目前都已呼叫 `recordTransaction()` 記營收：
  | 流程 | 收款確認點（員工端） | 交易 type |
  |---|---|---|
  | 入場 | `CheckinPage` 掃碼→確認入場／電話入場／已付費放行 | `checkin` |
  | 租借 | `RentalsPage` 確認收款/取件 | `rental` |
  | 銷售 POS | `SalesPage` 結帳 | `product` / `productSales` |
  | 課程 | 待辦頁待收款確認（現金/轉帳）／報名現金 | `course` |
  | 定期票 | `POST /passes`／入場 buy_pass | `pass` |

---

## 1. 硬體：WinPOS WP-560（已查證可串）

- **機型**：WinPOS WP-560 **二聯式發票印表機**（配件性質，需搭主機/PC 驅動；非獨立收銀機）。
- **列印方式**：**9 針點陣撞擊式**（impact，非熱感）→ 適合二聯式複寫紙；速度約 3 行/秒。
- **介面**：**RS-232 序列埠** ＋ 並列埠。現代 PC 需 **USB-to-serial 轉接線**。
- **指令集**：**ESC/POS、TP-3688、HPC-2000、IPT-2009 多模式，用 DIP 開關切換** → 走 **ESC/POS（公開標準指令集）** 即可程式驅動（關鍵，不是只能手按的機型）。
- **能力**：二聯式（存根聯＋收執聯）同時列印、證明聯單獨列印、自動定位/裁切、缺紙/卡紙/定位錯誤警示、可接錢櫃、內建中文字庫（16×9 點陣）。
- 參考：momo `i_code=8386654`、大山事務機 `dasan.com.tw/product/wp-560/`、百揚 Ushow POS、RS232 線材 winsmaker（WP520/WP560/WP103S 專用）。

## 2. 關鍵現實：紙本發票的「號碼」怎麼管

- 二聯式紙本統一發票的**號碼是預先印在財政部配號的專用發票紙捲上**。WP-560 只做「**自動定位**」：把日期/金額/品項印到當前那張預印號碼的發票上，**機器本身不產生號碼**。
- 因此號碼由**系統計數**管理：
  1. 店員**換紙捲/換期別**時，把該捲**起始發票號**輸入系統一次（含字軌）。
  2. 每成功列印一張 → 系統把號碼記到該筆交易、**計數 +1**。
  3. **作廢**（打錯）→ 該號標作廢、跳號。
  4. 每日結帳起訖號**自動帶出**（取代現手動抄號）；月銷售/發票明細自動產生。
- ⚠️ 風險：系統計數與實體紙捲**必須對齊**。要有「目前號碼」顯示 + 換捲重設 + 對不上時可手動校正的機制。

## 3. 橋接：RS-232 → 網頁員工端 → **決定走「本地列印代理」（方案 B）**

員工端是網頁（Firebase Hosting），無法直接驅動序列裝置。**已拍板走方案 B 本地代理**（2026-07-21）。

| 方案 | 做法 | 決定 |
|---|---|---|
| A. 瀏覽器 Web Serial API | Chrome 直接開序列埠送 ESC/POS | ❌ 不採用 |
| **B. 本地列印代理程式** | 櫃檯 PC 裝小程式開 COM 埠、開 `localhost` HTTP 給網頁呼叫、翻譯成 ESC/POS | ✅ **採用** |

**硬體規劃（使用者）**：買一台**舊發票機**測試、**RS-232 轉 USB** 接櫃檯電腦、已有**二聯式練習紙捲**。

### 3.1 本地代理設計
- **技術**：Node.js（與後端同語言）＋ `serialport`（開 COM 埠、送 bytes）＋ `express`（開 `http://localhost:<PORT>`）。
- **平台：Windows**（櫃檯電腦）→ 打包成 Windows 執行檔（`pkg`）或裝成 **Windows 服務**（如 `node-windows`/nssm）開機自啟；COM 埠走 Windows 命名（`COM3`…，RS-232轉USB 會配一個 COM 編號，裝置管理員可查）。
- **端點**：
  - `POST http://localhost:<PORT>/print` — 收發票內容 → 組 ESC/POS → 寫入 COM 埠 → 回 `{ ok, error? }`
  - `GET  /status` — 連線/缺紙/COM 埠狀態
  - `POST /void`（如機型支援）／或作廢僅由後端標號碼
- **呼叫方**：員工端網頁（同一台櫃檯 Chrome）`fetch('http://localhost:<PORT>/print', …)`。
- **⚠️ 瀏覽器限制（要處理）**：HTTPS 頁（`staff.redrocktaiwan.com`）呼叫 `http://localhost`——localhost 是安全內容例外**不會被 mixed-content 擋**，但新版 Chrome 的 **Private Network Access** 會發 preflight，代理需回應 `Access-Control-Allow-Private-Network: true` ＋ CORS 允許該來源。此為已知可解的細節，代理端設定即可。
- **設定**：COM 埠編號、baud rate、DIP 對應的發票定位參數，放代理的設定檔（每館櫃檯各自設）。
- 列印介面抽象見 5.1（`InvoicePrinter` adapter `localagent`）。

## 4. UI 整合（二聯式較單純）

- 二聯式＝**B2C，無載具/捐贈** → 收款確認流程乾淨。
- 做一個共用流程 **「收款確認 → 送印 → 記號碼」**，四頁共用：

  `入場/租借/銷售/課程 收款確認` → 組發票內容（日期＋金額＋品項；租借**只租金不含押金**；**選填統編**） → 送 ESC/POS 列印 → WP-560 印在當前發票 → 系統記該張號碼＋計數 +1。

- **統編（選填）**：二聯式**有時客人也要印統編**（報帳） → 收款確認的共用元件加**選填統編欄**（8 碼），有填就印在發票上、存進 `invoices.buyerTaxId`。多數 B2C 留空。（真正 B2B 三聯式格式日後要再擴充。）
- **作廢/退費連動**：現有取消/退費流程 → 能作廢就作廢、否則直接退費（不折讓，詳見第 4.1 節）。
- **錢櫃驅動**：WP-560 有**錢櫃輸出埠**（錢櫃接發票機、非接 PC），代理用 ESC/POS `ESC p`（`0x1B 0x70`）開櫃。`InvoiceCheckout` **現金收款才帶開櫃**（電子/轉帳不開）；現金退費、每日結帳點鈔可另給「開錢櫃」鈕。代理端點加 `POST /open-drawer` 或 `/print { openDrawer:true }`。
  - 🚨 **硬體安全警告（2026-07-31，WP-560 說明書原文）**：「**注意：請勿使用 TP-3688 相容錢櫃，否則將造成印表機及錢櫃嚴重毀損**」。TP-3688 是這台印表機**出廠預設**的協定（本案已撥 DIP2#3 從 ON→OFF 切到 ESC/POS 相容模式，見 §5.5）；TP-3688 與 ESC/POS 是兩套不同電路訊號規格（觸發電壓/極性/脈衝方式不同），**接錯類型的錢櫃有實際燒毀硬體的風險，不可用試的**（跟純軟體指令試錯的風險層級不同）。**接任何錢櫃前，務必先確認該錢櫃是「標準 ESC/POS(Epson) 相容」而非「TP-3688 相容」**（多半要查該錢櫃/該收銀機的原廠說明書或問經銷商）。
  - ✅ **WP-520（同系列機型）錢櫃接腳規格確認（依說明書）**：**RJ12（6P6C，6 線）**——外殼地／錢箱輸出1／微動開關（偵測錢櫃開闔狀態）／電源 +24V／錢箱輸出2／邏輯地。這是業界通用的「Epson 相容型」標準錢櫃接法。
  - 📌 **實務決定：市面上只找得到 RJ11（4線）錢櫃 → 直接採用**：RJ11 插頭可physically插入 RJ12 插座（形狀相容，只是少接 2 條線）——跟 ACCUPOS「接頭形狀完全不同」是不同等級的狀況，屬「軟性風險」（頂多缺少微動開關的錢櫃開闔偵測功能、或基本開櫃也失敗，實測便知），**非**燒毀硬體等級的風險。且本案**只需要 `ESC p` 觸發彈開、不需要微動開關的偵測回報**，即使 RJ11 少接的剛好是那 2 條線也不影響需求。**結論：買 RJ11 錢櫃即可，正式接線時實測開櫃功能是否正常**。
  - ✅（2026-07-31 已釐清，結論＝不接）**ACCUPOS A600 附贈錢櫃確認不相容、不接**：實際檢查兩邊接頭——WP-560/WP-520 錢櫃孔＝標準 RJ12 模組化方孔（6 pin，見上）；ACCUPOS A600 錢櫃線＝**三線（黑/綠/紅）、方形三孔接頭**，3 條線本來就裝不下上述 6 個獨立訊號、接頭形狀也完全不同，**物理上插不進去**。加上 TP-3688 混用警告、兩品牌同屬中文收銀機生態系（撞型號風險高），**結論：不接、不做轉接線嘗試**。正式上線改買一台**標準 RJ12、ESC/POS(Epson) 相容**的獨立錢櫃即可（市面普遍、便宜）；ACCUPOS A600 若仍要用，維持原廠主機+印表機+錢櫃配對使用。
- **補印**：交易/會員紀錄旁加「補印發票」（重印證明聯，不佔新號）。
- **開票失敗退路**：款已收、列印失敗（缺紙/斷線）→ 進「**待開立佇列**」，不卡收款；UI 顯示「發票待補印」，排除後補印。

## 4.1 退費 → 只做「作廢」，不做「折讓」（2026-07-21 定案）

> 使用者現行做法：退費一律**直接退錢**、不開折讓證明單。故本專案**不做折讓模組**（`allowances`、折讓單列印都不做），大幅縮小範圍。

| 退費情況 | 系統處理 |
|---|---|
| **能作廢**（**同期別**＋**全額**＋**收得回發票兩聯**，如打錯/當天取消） | 作廢該發票號、**跳號**、自動帶進結帳「作廢號碼/金額」（＝現手動記作廢自動化）；`invoices.status='void'` |
| **不能作廢**（跨期／部分退／發票收不回） | **直接退費**（現有負向沖銷退錢），原發票不動；系統標 `invoices.refunded=true` 留紀錄；**不開折讓** |

- 統一發票期別＝雙月（7-8月、9-10月…）；作廢僅限同期別內。
- **接現有退費流程**（錢的退費已由負向沖銷處理，此為加的發票層）：取消入場（多同日同期全額→作廢）／課程退費（多跨期/部分→直接退費）／試上取消（同期全額→作廢，否則直接退費）／比賽·租借退費（同判斷）。

⚠️ **財稅提醒**（使用者決定、非違法）：跨期/部分「直接退費不折讓」時，該筆銷售**營業稅未退回**（多繳）。**✅ 做「退費/作廢報表」**（給會計作帳）：每筆列 原發票號/金額/日期/類型（作廢 or 直接退費）/關聯訂單，可匯出 CSV/Excel 供會計月結對帳，補跨期直接退費的帳務可追溯性。

### 4.1.1 📋 未來政策（2026-07-31 提出，**待發票機正式串接後才實施，現在不動**）

> 使用者拍板：發票機正式上線後，**退貨/退費操作只需輸入發票號碼，系統自動查出對應的品項/金額並回填**（不用店員手動重新輸入一次品項明細）——**商品銷售（POS）退貨也適用同樣邏輯**。
> 技術上＝屆時 `invoices` 集合已有 `number`+`items`+`relatedType/relatedId` 完整記錄，退貨表單改成「輸入發票號碼 →（查 `invoices` 找到該筆）→ 自動帶出品項/金額，店員只需確認＋選作廢或直接退費」。**現階段完全不動 `SalesPage.jsx` 現有的 `returnSale`（需自行對照品項）退貨流程**，僅先記錄政策方向；等 §9 的手動發票 modal 進化成真正串接印表機、`invoices` 資料完整後再改。

## 5. 抽象設計（比照金流 adapter 模式）

### 5.1 前端列印介面（傳輸無關）
```
InvoicePrinter.print({ items, total, date, buyerTaxId, openDrawer }) → { ok }
InvoicePrinter.openDrawer() → { ok }        // 現金收款/退費/點鈔（ESC p）
InvoicePrinter.status() → { connected, paperOk }
```
- adapter：**`localagent`（採用，呼叫本地代理 localhost）** ／ `mock`(dev)。
- 共用元件 **`InvoiceCheckout`**：四頁收款確認共用，內含「送印 + 記號碼 + 失敗進佇列」。
- **號碼由後端管**（機器不回報號碼；紙捲號碼預印）：後端 `invoiceState.currentNumber` 為權威；每次列印成功後端計數 +1，代理只負責把資料印到當前紙上。

### 5.2 後端資料模型：新增 `invoices` collection（草案）
```
invoices/{id} {
  number, track,             // 發票號碼、字軌
  gymId, date,
  amount,                    // 開立金額（不含押金）
  items: [{name, qty, price}],
  buyerTaxId,                // 選填統編（二聯有時要印；B2C 留空）
  relatedType, relatedId,    // checkin / rental / product / course / pass
  status: 'issued'|'void',
  refunded,                  // 不能作廢但已直接退費（跨期/部分/收不回）→ true，原發票不動
  voidReason, voidedAt,
  reprintCount,
  createdBy, createdAt,
}
```
- 每館一組「**目前發票號碼狀態**」：`gyms/{gymId}.invoiceState { track, currentNumber, rollStart, updatedAt }`（換捲重設）。
- 每日結帳改**讀 `invoices`** 算起訖/作廢，取代手動輸入（保留手動校正欄位當退路）。

### 5.3 收款點接線（同金流 orderType 對照）
| 流程 | 觸發點 | 開票金額來源 |
|---|---|---|
| 入場 | `confirmCheckIn` / `/checkin/phone` / `/direct` | `amountPaid`（含加購） |
| 租借 | `/rentals/:id/confirm`（現金）/ transfers rental 確認 | `totalRentalFee`（**排除押金**） |
| 銷售 | `/products/sell` | 品項合計 |
| 課程 | 待收款確認（transfers course / 現金） | 報名費 |
| 定期票 | `POST /passes` / buy_pass confirm | 票價 |

### 5.3.1 📋 未來政策（2026-07-31 提出，**待發票機正式串接後才實施，現在不動**）

> 使用者拍板：**等到可以串接發票機列印後，所有「預收款項」類型的營收計算，全部改成併入「開立發票當天」計算營收**（而非目前依各流程「認列時點」記帳的 accrual 模式）。目的：讓系統報的營收數字跟實際印出去的發票日期對齊（稅務/會計對帳用）。

- **影響範圍（現行 accrual 認列時點，供屆時比對哪些要改）**：
  - **課程整期報名**：`enroll-all` 報名當下即 `recordTransaction`（認列在最後一堂日期），非逐堂認列。
  - **分期付款**（課程/定期票/續約）：首期收款當下記帳，後續期數各自到期收款時才記。
  - **比賽報名費**：報名當下（或轉帳確認收款當下）記帳，早於比賽實際舉辦日。
  - **定期票／回數票**：售出當下記全額，即使效期橫跨數月。
  - **入場/租借/POS**：本身就是**當下收當下記**，性質上已經等於「開票當天」，這條政策對這三者**幾乎無影響**（本來就對齊）。
- **待決定的技術細節（發票機正式串接、開始設計 `invoices` 集合時再處理）**：現行 `recordTransaction` 認列時點的程式碼是否要**整段改成看發票開立時間**、或是維持 accrual 記帳但另外做一層「依發票日期重新分組」的報表視圖（兩種做法對日結帳/月報表/退費沖銷的影響不同，需要屆時重新評估）。**現階段完全不動任何程式碼**，僅先記錄政策方向。

## 5.4 SDK / 指令手冊現況（2026-07-21 查證）

- WP-560 走 **ESC/POS 標準指令集**（另可 DIP 切 TP-3688/HPC-2000/IPT-2009）。ESC/POS 是**公開標準**（`ESC` 0x1B / `GS` 0x1D 起始）→ **基本列印（文字、金額、對齊、切紙、開錢櫃）可直接依公開 ESC/POS 手冊實作**，不需原廠 SDK。
- 但**沒有在網路上找到 WP-560 官方完整 SDK / 程式手冊**。社群有用 ESC/POS 驅動同系列 WinPOS 出據機的實例（Delphi × ESC/POS × WP-T810、POS 列印電子發票證明聯）可參考做法。
- **定位可自己做（靠試印校正，非必須原廠規格）**：二聯式發票的定位＝**游標控制**（換行往下 N 行、`ESC $` 絕對水平位置/空格右移 N 字）。9 針點陣固定字距，位置＝(行, 欄) 好算；發票格式財政部標準、紙捲尺寸固定 → 每欄位相對表頭的偏移量**固定、量一次寫死**；機器「自動定位」負責對齊每張表單起點，我方只管表單內欄位偏移。用**練習紙捲印→看歪→調偏移→再印**收斂即可。
- **結論**：基本列印＋定位**皆可自幹**（公開 ESC/POS + 試印校正）。經銷商的**定位範例/DIP 設定只是加速**（省初始偏移與 DIP 模式試錯），不是動工必要條件。
- ⚠️ 真正的風險點：舊機的**自動定位感測器**若異常/紙捲無定位標記，表頭對齊會較麻煩（買到機器一試即知）——這是 P1 PoC 要先確認的。

## 5.5 P1 PoC 實測結果（2026-07-31，舊機＋USB轉RS232＋練習紙捲，已過最大技術風險點）

> 用 `scratchpad/printer-test/`（Node + `serialport`，throwaway、不進版控）直接送 raw ESC/POS bytes 到序列埠測試，逐步排查後**確認可穩定列印中文**。以下為實測得出的關鍵設定，日後寫正式本地代理程式**直接套用即可、不用重踩**。

- ✅ **實體連線**：USB-RS232 轉接線裝置為 `/dev/cu.usbserial-DU0ERYQM`（Windows 上會是 COM 埠），**baud 9600、8 data bits、no parity、1 stop bit**。序列埠開啟/寫入本身完全正常、無需特殊設定。
- ✅ **DIP2 開關第 3 顆（機器底部）須撥到 OFF**：出廠預設是 TP-3688 專屬指令集，這台不撥開關的話**完全不理會 ESC/POS 指令、連進紙都沒反應**；撥到 OFF 後才會開始解讀 ESC/POS bytes（可聽到馬達進紙聲音當作已生效的訊號）。DIP1/DIP2 兩組開關都在機身底部（斷電後翻過來看，開關很小可用原子筆尖撥動）。
- ⚠️ **面板紅燈是這台正常狀態、不是錯誤燈號**——先前排查了紙張感應器/紙蓋/存根聯回收捲軸繞法都正常，最後才確認「開機從頭到尾都是紅燈」對這台機器來說是正常現象，**不要拿它當診斷依據**、以實際列印結果為準。
- 🐞 **這台的 ESC/POS 指令集是有限子集，以下指令會產生亂碼、確認不支援，勿使用**：
  - `ESC G n`（雙重列印）、`ESC E n`（加粗）——送出後**不會被當指令消化**，`ESC` 位元組被忽略、後面的英文字母（`G`/`E`）被當成一般文字印出（且會印成**全形**字元，如「Ｇ」「Ｅ」），造成亂碼。
  - `FS &` / `FS .`（Kanji 模式切換）——同樣不支援，送出後在該行前後產生亂碼字元。
- ✅ **中文列印做法（已驗證乾淨可用）**：**完全不需要任何模式切換指令**——`ESC @`（初始化）之後，直接把中文字串用 **Big5** 編碼（`iconv-lite` 的 `iconv.encode(str, 'big5')`）送過去即可正確印出中文，中英文混排也沒問題。**切勿用 `Buffer.from(str)`**（Node 預設 UTF-8）送含中文的字串，兩種編碼對不上會印出亂碼——這是這次測試踩到的第一個雷，錯誤出在測試腳本本身、不是印表機的問題。
- ✅ **`GS ! n`（字體放大，如 `0x11`＝寬高各 2 倍）指令有效**：這台的色帶（IR-71 相容）已偏舊、正常大小印出的字偏淡但**堪讀**（使用者實測「還可以」）；如需加強清晰度，放大字體（`GS !`）是目前唯一驗證有效的加強手段（`ESC G`/`ESC E` 雙重列印/加粗都不支援，見上）。**建議日後正式上線前更換一捲新的 IR-71 相容色帶**（網路上幾百元可買到），現況已堪用、非阻塞項。
- ✅ **紙張機構確認正常**：紙蓋扣緊、無需額外壓桿、進紙定位正常、存根聯（副聯）回收捲軸繞接方式也正常——這些先前擔心的「二手機物理故障」皆已排除。
- 📌 **正式代理程式（P1 之後的 P3/P4）撰寫時的具體套用**：初始化只送 `ESC @`；中文/英數混合字串一律先 `iconv.encode(str,'big5')` 再送位元組（勿用預設 UTF-8）；不要用 `ESC G`/`ESC E`/`FS &`/`FS .`；如需加粗視覺效果改用 `GS !`（字體放大）；換行/定位游標控制（`ESC $`／多個 `\n`）等基本指令未在本次測試範圍內驗證，等實際刻二聯式發票版面時再逐一試印校正。
- ✅ **重大發現：`0x0C`（ASCII Form Feed 換頁指令）＝自動對位＋裁切！**——一開始連續送兩次列印（中間只用 `\n\n\n\n` 分隔）**沒有**自動對位/裁切，一度以為要自己算行數校正；後來測手動進紙鍵**按著不放會自動停在下一張起點並裁切**（證實定位感測器硬體正常），改送 `0x0C` 這個標準換頁指令（非 ESC/POS 專屬、是最基本的 ASCII 控制字元）→ **印表機自動進紙、對位、裁切，跟手動按鍵行為一致**！這代表**不需要自己算換行行數校正定位**，每張發票內容印完後**直接送一個 `0x0C` 位元組**即可自動跳到下一張正確起點並裁切好——大幅簡化原本預期要做的「試印校正」工作。
- 📌 **正式代理程式套用**：每張發票列印流程＝`ESC @` 初始化 → 中文/英數內容（Big5 編碼）→ **`0x0C` 結尾** → 自動對位+裁切完成，下一張直接可印，不需手動管理行數/偏移。

## 6. 分階段實作計畫（草案）

1. **P0 取得資源（進行中）**：使用者買**舊發票機**＋**RS-232轉USB**＋已有**二聯練習紙捲**；另向經銷商問**發票定位/DIP 設定**（ESC/POS 基本指令用公開手冊即可）。
2. **P1 本地代理 + 列印 PoC**：先做**本地代理**（Node＋serialport＋express localhost）→ 用練習紙捲印出一張二聯式測試發票，把**定位/裁切/中文**調到對位（DIP＋試印校正）。這步是整個專案的技術風險點，**已於 2026-07-31 用 raw ESC/POS 測試腳本全部驗證通過**（見 §5.5）：通訊協定（DIP2#3 撥 OFF＋Big5 直送中文）、對位/裁切（每張結尾送 `0x0C` Form Feed 即自動對位+裁切，免自算行數）。剩**表單內欄位相對偏移**（日期/金額/品項印在表單哪一行第幾欄）待正式刻版面時用練習紙捲逐步校正。
3. **P2 號碼管理**：`invoices` 集合 + 每館 `invoiceState`（換捲設起始號、自動計數、作廢跳號、目前號碼顯示/校正）。
4. **P3 共用元件**：`InvoiceCheckout` + `InvoicePrinter` adapter；先接一個流程（銷售 POS）端到端。
5. **P4 四流程接線**：入場/租借/課程/定期票依序接；失敗佇列 + 補印。
6. **P5 結帳自動化**：每日結帳改讀 `invoices` 自動帶起訖/作廢；月銷售/發票明細自動產生；手動輸入降為校正退路。
7. **P6 作廢/退費連動**：取消/退費流程接作廢（能作廢則作廢+跳號；否則標 refunded 直接退費，不折讓）。
8. **P7 退費/作廢報表**：列每筆退費（原發票號/金額/日期/類型/訂單），匯出給會計月結對帳。

## 7. 決策紀錄 / 待辦

**已定（2026-07-21）**：
- ✅ 走**實體二聯式紙本發票**，不走電子發票/加值中心。
- ✅ 橋接走**本地列印代理（方案 B）**，不走 Web Serial。
- ✅ 硬體：買**舊發票機**＋**RS-232轉USB**＋**二聯練習紙捲**（使用者備）。
- ✅ 二聯式**加選填統編欄**（客人要報帳時印）；三聯式 B2B 日後再說。
- ✅ 號碼由**後端計數管理**（換紙捲設起始號、每印 +1、作廢跳號）。
- ✅ 開票時機＝各流程**收款確認當下**。
- ✅ **退費只做「作廢」、不做折讓**（能作廢就作廢跳號，否則直接退費、原發票不動）。
- ✅ WP-560 可**驅動錢櫃**（現金收款/退費/點鈔開櫃，ESC p）。
- ✅ **做「退費/作廢報表」**給會計作帳（可匯出）。
- ✅ 櫃檯電腦＝**Windows** → 本地代理打包 Windows exe/服務、COM 埠。

**待辦 / 待提供**：
1. ✅（2026-07-31 完成）舊發票機＋轉接線到位、DIP2#3 已撥 OFF、通訊協定＋中文列印已實測通過（見 §5.5）。
2. ✅（2026-07-31 完成）發票版面定位/裁切試印校正：確認**每行 24 個半形字元**（12 個中文全形字）、`0x0C` 觸發自動對位+裁切（見 §5.5）；已試出可用版面草稿——公司抬頭＋館別合一行、統編/地址置中、地址依行政區斷行、品項＋合計、買受人統編**選填才印該行**、收尾防偽線＋「以下空白」。**尚未做**：頂端固定留白的精確量測（目前知道有~2行留白但無法消除，設計上直接當作天然邊界）、真實二聯式財政部號碼紙捲上的實測（今天都是空白練習紙捲）。
3. 本地代理 Windows 打包/服務化與開機自啟（正式寫代理程式時一併做）。
4. 建議日後更換一捲新的 IR-71 相容色帶（現況偏淡但堪用，非阻塞項）。
5. ✅（2026-07-31 已釐清，結論＝不接）**ACCUPOS A600 附贈錢櫃確認不相容、不接**——詳見 §3 錢櫃驅動小節；改買標準 RJ12/ESC-POS 相容獨立錢櫃即可。

## 8. 列印時機／方式（2026-07-25 提出，**待後續討論拍板**）

> 文件原只寫「開票時機＝各流程收款確認當下」；深入後有三個情境需拍板，先記下，之後討論。

### 待決策（時機）
1. **情境 A 現場付款（入場/租借/POS）**：客人在櫃檯按確認收款→當下印、收執聯直接給。✅ 已定、無爭議。**入場加購岩鞋/粉袋本就同一筆 `checkin` 交易**→天然一張發票含入場＋加購。
2. **情境 B 一次來訪多筆消費**（入場 vs 另在 POS 買鞋）：
   - 選項1（推薦）**逐筆開票**：入場一張、POS 一張，跟現有交易結構一致、工程小。
   - 選項2 **合併一張**：需跨流程購物車/掛帳，大工程。
   - → **待拍板**。
3. **情境 C 非臨櫃付款（轉帳/LinePay 遠端，客人不在場）**（課程/比賽/體驗/入隊/轉帳，staff 事後在待辦頁確認收款）：
   - 建議：**發票在「確認收款當下」開立**（發票日對到收款日、帳務正確）→ **收執聯放「待領發票」夾** → 客人**下次到場／首堂課報到時交付**（不郵寄）。
   - → **待拍板**。
4. **情境 D LinePay 入場（pay-first，付了沒入場→轉 30 天單次券）**：
   - 建議：**發票一律在「到場掃碼確認入場」當下印**（付款當下人不在櫃檯、無印表機）；未入場轉券者→**等他持券來入場、掃碼那刻才印**（發票日＝實際使用日）。
   - → **待拍板**。

### 方式（多屬既定、一併確認）
- **二聯同印**（存根聯＋收執聯）；收執聯給/待領、存根聯留存。
- 內容：日期＋金額＋品項（租借**只租金不含押金**）＋**選填統編**（8 碼，報帳才填）。
- **補印**：只重印收執聯、不佔新號。
- **開票失敗**（缺紙/斷線）→ 待開立佇列，不卡收款。
- **錢櫃**：現金收款才 `ESC p` 開櫃；電子/轉帳不開。

> 👉 下次討論就決定 B/C/D 三條，決定後補進「已定」清單並更新第 4/5.3 節接線。

## 9. 手動「開立發票」modal（2026-07-28，**已提前上線、非硬體綁定**）

> ⚠️ 與第 1-8 節的「WP-560 實體印表機自動列印」計畫**不同層次**——這是**先做的人工過渡版**：店員仍用現有（非串接）發票機手寫/手動印二聯發票，系統只負責**記下開了哪張、多少錢、統編、備註**，並自動計入當日結帳「加減項」（`+發票開立`／作廢時 `-發票作廢` 對稱沖銷），**不動原本已認列的營收交易**（避免重複計算）。等第 1-8 節的印表機真正接上後，`InvoiceModal` 的「開立」動作可直接改呼叫 `InvoicePrinter.print(...)` 觸發實際列印，UI/資料模型不必大改。

**已上線（2026-07-28，commit `a0a4035`／`ee0aa6b`／`6377cb7`／`9d38c94`／`ff76b69`）**：
- 共用後端 `src/services/invoiceService.js`：`invoiceRecords` 集合，`sourceType+refId` 決定唯一性（同一筆訂單同時最多一張 `status:'issued'`，須先作廢才能重開）。
- 共用前端 `src/components/InvoiceModal.jsx`：日期時間/品項/金額(預填實收金額)/統編(選填)/備註 → 開立；已開立時只顯示摘要＋「作廢」鍵（二次確認）；**「關閉」鈕永遠都在**（開立表單狀態＝取消不送出、已開立狀態＝單純關閉），即本輪要求的「都要有取消按鈕」原則已內建於共用元件本身，之後每加一個新流程都天然滿足。
- **課程**：入口在「**課程學員總表**」（`MembersPage.jsx` 課程學員 tab，每位學員一顆「🧾 開立發票」）。**刻意不在「待收款確認」當下開票**——2026-07-28 使用者明確拍板：課程發票統一由課程學員總表開立，待收款確認流程維持原樣（只是確認金流狀態，不觸發發票）。
- **比賽**：入口有兩處——報到頁（`CheckinPage` 掃碼報到後）＋ 報名總名單列（`CompetitionsPage`，不用點進報名詳細資料）。

**待擴充（使用者 2026-07-28 拍板，**之後執行**，做法沿用同一套 `invoiceService`+`InvoiceModal`，加新的 `sourceType` 即可，非工程難題只是排期）**：

| 流程 | 帶出「開立發票」modal 的時機 |
|---|---|
| **商品銷售（POS）** | `SalesPage` 完成銷售送出成功後 |
| **器材租借** | `RentalActionModal` **確認歸還**（action='return'）成功後——⚠ 不是確認收款/取件，是**歸還**確認時 |
| **入場 QR** | 掃碼／確認入場（`confirmCheckIn`）之後 |

**2026-07-31 進度**：
- ✅ `invoiceService.createInvoice` 的 `memberId` 改為**選填**（原本 `MISSING_FIELDS` 會擋）——POS 銷售常見**匿名交易**（`SalesPage.jsx` 的 `sellProducts` 本就允許 `memberId:null, memberName:'匿名'`），開票不應強制要求會員綁定。此修正安全、向後相容（課程/比賽本就一定有 memberId，不受影響）。
- 🔨 **入場（checkin）、商品銷售（POS）兩流程實作中**：確認採用**跟課程/比賽同一套「手動發票按鈕」模式**（非自動彈窗）——入場確認成功／銷售完成後，畫面上出現一顆選填的「🧾 開立發票」按鈕，店員可選擇是否點開記錄；**不點完全不影響任何現有流程**。
- 📌 **使用者提出但明確延後的加強功能（等 Windows 正式部署本地代理後才做）**：送出「開立發票」後，若判斷為**現金支付**，串接本地列印代理**觸發實際列印＋自動開錢櫃**，成功後**自動關閉 modal**（並讓該筆訂單的「開立」動作自然變成已開立摘要狀態，不能重複開立、需先作廢才能重開——這點`InvoiceModal`現有邏輯已滿足，不需額外開發）。**現階段（本地代理仍是跑在開發者 Mac 上的原型、非正式 Windows 部署）不接這段，只留手動記帳版**。
- 📋 **未來營收認列時點政策**：見 §5.3.1（發票機正式串接後，所有預收款項營收改按開票日期計算，現在不動）。

- **總原則**：**所有會產生收入的流程**最終都要有對應的開立發票入口；**取消/略過鈕為必備**（`InvoiceModal` 已內建，新流程只要正確接上共用元件即自動符合）。
- 課程／比賽的既有兩個入口（課程學員總表／報到頁+報名名單列）已經是這個原則下的兩個實例，之後 POS/租借/入場 QR 只是把清單補齊，屬同一套設計、非新設計。

> 機器到位後即可進 **P1（本地代理 + 列印 PoC）**——先把二聯式定位/裁切試印調對，再往上做號碼管理、`InvoiceCheckout` 共用元件與四頁接線。
